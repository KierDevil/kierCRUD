# Department Financial Records Frontend

Vue 3 + Vite + Vuetify + Axios frontend.

Run from `C:\Kier` or `C:\kierCRUD`:

```powershell
npm install
npm run dev
```

The root `start.cmd` launches this frontend and the ASP.NET API together.
